node_modules
storybook-static
.env
.env.local
github.pem

temp/

# Local clone of the full VTEX agent skills catalog (only the vendored
# skills under .agents/skills are versioned here)
.agents/vtex-skills/
